import React from 'react';

function DemonstrationIntro(){
  return (
    <div>
      <h1>Demonstration</h1>
      <p>The below demonstrates using a RESTful API call to update the state of a React component. What makes React particularly useful is that it only updates when there has actually been a change to the DOM. </p>
    </div>
  )
}

export default DemonstrationIntro;
